name: beautifulwebsite
description: A new Flutter project.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.1 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  google_fonts: 4.0.4

dev_dependencies:
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true